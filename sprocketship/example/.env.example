# Sprocketship Example - Environment Variables Template
# Copy this file to .env and fill in your Snowflake credentials
# Make sure to add .env to your .gitignore to avoid committing credentials

# Snowflake Account Information
# Format: <account_identifier>.<region> (e.g., xy12345.us-east-1)
SNOWFLAKE_ACCOUNT=your_account_here

# Snowflake Authentication
SNOWFLAKE_USER=your_username_here
SNOWFLAKE_PASSWORD=your_password_here

# Default Connection Settings
SNOWFLAKE_ROLE=sysadmin                    # Default role (can be overridden per procedure)
SNOWFLAKE_WAREHOUSE=compute_wh             # Warehouse to use for connections
SNOWFLAKE_DATABASE=example_db              # Default database for procedures
SNOWFLAKE_SCHEMA=public                    # Default schema for procedures

# Notes:
# - The role specified here is the default connection role
# - Individual procedures can override this with 'use_role' in config
# - Make sure your user has appropriate privileges for the operations
# - For this example, you'll need at minimum:
#   - SYSADMIN role or equivalent for database/warehouse operations
#   - USERADMIN role or equivalent for role management operations
